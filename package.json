{
  "name": "dLUploadHost",
  "version": "0.5.0",
  "private": true,
  "description": "Your own link shortener that's easily deployed as a static site (for free)",
  "homepage": "https://github.com/JohnMiltonHacks/dLUploadHost#readme",
  "bugs": "https://github.com/JohnMiltonHacks/dLUploadHost/issues",
  "license": "MIT",
  "repository": "JohnMiltonHacks/dLUploadHost",
  "scripts": {
    "build": "npm run clean && NODE_ENV=production eleventy",
    "clean": "del _site",
    "dev": "eleventy --serve",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write .",
    "release": "release-it"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "del-cli": "^4.0.1",
    "prettier": "2.7.1",
    "release-it": "^15.0.0"
  },
  "engines": {
    "node": ">=14"
  }
}

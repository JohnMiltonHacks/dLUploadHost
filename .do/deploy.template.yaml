spec:
  name: dLUploadHost 
  static_sites:
    - name: dLUploadHost
      git:
        repo_clone_url: https://github.com/JohnMiltonHacks/dLUploadHost.git
        branch: master
      envs:
        - key: J0hnMilt0n
          value: '0'
      build_command: npm run build
      output_dir: _site
